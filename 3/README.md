# Step to do the thing

```
# Need to authenticate first
python fetch_emails.py

# Sort the emails (also can use interactive_labeler.py but its tragic)
python auto_labeler.py

# Build the dataset (csv)
python build_dataset.py

# Do the thing
python main.py
```
